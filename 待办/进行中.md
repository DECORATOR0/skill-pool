# 进行中

## 当前最高优先级

- [X] 回看并确认实验中各个智能体的流程行为：
  - 先把 `router / executor / critic / actor / skill` 在 `mode1 / mode2` 中的真实调用链、跳过条件和交接关系看清楚
  - 这件事优先于继续讨论更细的错误归因或 prompt 微调
- [ ] 初步检查现在 `model2 / mode2` 是否还有正在发生、且需要尽快处理的问题：
  - 先看当前新 run 的盘面
  - 再判断是否存在明显卡点，例如参数生成链、planner-runtime、空响应或新的结构性错误
- [X] 回看并整理“并行训练 / 并行产出 candidate skill 再压缩”的相关资料与支撑：
  - 已实现 `model3` 的“每题隔离训练 + 成功后晋升 + 失败直接丢弃”链路
  - 已补独立 `prompts_model3/` 与 `train-model3` 命令
  - 已完成 2 题 smoke test，并发 `50` 的正式 run 已发起
- [ ] 回来后先快速扫一遍 `model1 / mode1` 的错误模式：
  - 先判断哪些错误已经只是观察项
  - 哪些错误属于需要尽快解决的高优先级问题
  - 优先关注会污染 skill 库、反复误导路由、或阻断后续训练的错误类型

## 当前主线

- [ ] 完成双模式实验的正式运行：
  - `mode1`：executor-oriented skill 的训练与评估
  - `mode2`：planner-oriented skill 的训练与评估
- [ ] 跟进 `model3` 并行 success-only 训练 run 的盘面与最终产物：
  - 正式 run：`project_skills/runs/model3_parallel/20260321_2347_qwen3_8b_paid_model3_parallel_train`
  - 重点看 `successful_skill_summary.json`、`successful_skill_library/` 和各 `task_xx/` 的 `task_summary.json`
  - 确认成功 skill 数量、失败任务丢弃策略、以及后续聚类/压缩入口是否满足预期
- [ ] 在实验结束后，优先用 AI 并排分析两组结果：
  - 整体性能如何
  - 稳定性如何
  - skill 库生成效果如何
  - skill 复用、合并和上限利用情况如何
- [ ] 如果结果不理想，优先考虑是否要收缩、改写甚至部分去掉当前 prompt 设计，而不是继续叠加 prompt 复杂度

## 实验结束后的重点分析

- [ ] 比较 `mode1 / mode2` 的训练表现、评估表现和最终 skill 库质量
- [ ] 观察训练过程中 `critic / actor` 的真实行为：
  - 是否每个 iteration 都会调用
  - 成功任务、失败任务、无匹配 skill 时各自怎么处理
  - 哪些路径会跳过 `critic` 或 `actor`
  - 产出的 reward / action selection / create / modify / merge 行为是否符合预期
- [ ] 检查 `mode2` 生成出的 skill 是否真的体现 planner-oriented 先验，而不是把更抽象的措辞重新塞回 executor
- [ ] 检查 `mode1` 在测试阶段是否会因为 executor-facing skill 写法和 `skill_count_limit = 6` 而变得过于死板
- [ ] 判断后续主线应继续保留双模式并行，还是淘汰其中一种范式

## 后续可能动作

- [ ] 评估“主流程 skill + helper modules”替代“单轮只选一个 skill”的可能性
- [ ] 评估并发训练的可能性：
  - 是否可以先并行产出一条一条候选 skill
  - 再在后处理阶段统一去重、合并和整合 skill 库
  - 对比这种方式与当前串行训练在速度、稳定性和 skill 质量上的差异
- [ ] 设计 skill 的降权 / 冻结 / 剔除 / 回退机制，避免坏 skill 反复命中
- [ ] 如果双模式实验显示 prompt 复杂度收益不高，优先做 prompt 收缩，而不是继续堆叠 prompt

## 维护规则

- 这份文件只保留当前主线，不再重复列代表样本；样本保留理由统一写进 `实验记录/实验总表.md` 或对应实验记录
- run 有新增或结论有变化时，先更新 `实验记录/实验总表.md`
- 与双模式实验相关的长期观察，继续同步到 `AI交互记录/` 对应日期文件
- TODO 状态变化后，再同步到本文件或 `待办/已完成.md`
