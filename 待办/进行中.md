# 进行中

## 当前最高优先级

- [ ] 今天已重新发起并继续跑完 `model3` 正式并行训练：
  - 昨晚已手动停止 `model1 / mode1`、`model2 / mode2` 和 `20260323_2222_qwen3_8b_paid_gpt52_model3_parallel_train`，避免继续空耗
  - 不再继续 `model1 / mode1` 和 `model2 / mode2` 的长跑；后续除非明确需要补对照，否则不再投入主要算力
  - 主线只保留 `model3`：`每题隔离训练 + success-only 晋升 + 失败直接丢弃`
  - 今天已按原 run `20260323_2222_qwen3_8b_paid_gpt52_model3_parallel_train` 用 `--resume` 恢复启动，跳过已完成题，其余题继续跑
  - 当前方案继续用付费 `qwen3-8b` 跑 `router / planner / executor / parameter_worker`，`actor / critic` 改用 `gpt-5.2`
  - 并发继续按 `150` 跑，目标先把整轮 `248` 题跑完整
- [ ] 发起 `model3` 并行训练后，补做 Linux 侧维护：
  - 放在并行训练启动之后处理，不抢主线启动时间
  - 维护内容按原来的 Linux 主机/服务维护安排继续推进
- [ ] 在 `model3` 并行训练继续运行的同时，同步看 skill 整合/聚类/压缩方向的论文和方案：
  - 重点看“多 candidate skill 聚类后抽象”“tool-using agent skill abstraction”“成功轨迹压缩为少量可复用策略”这类方向
  - 输出目标不是泛泛读论文，而是为后续“把大批成功 skill 整成 6 个优良 skill”提供可执行结构
- [ ] 顺手让 AI 把之前散落文档洗一遍，压成更干净的长期记录：
  - 重点清理 `AI交互记录/`、实验记录和零散说明
  - 把仍有长期价值的约定、阶段结论和后续动作收束到更少的文档里
- [ ] 明确评估阶段默认走并行，而不是串行：
  - 整合后的 skill 库在评估阶段应视为只读
  - 每题输出目录和临时目录继续隔离
  - 优先把评估当成“高并发只读推理”，而不是继续沿用训练阶段的保守串行心智

## 当前主线

- [ ] 跑完一整轮 `model3`，拿到尽可能完整的 success-only skill 池：
  - 重点看 `successful_skill_library/`
  - 重点看 `successful_skill_summary.json`
  - 重点看各 `task_xx/` 的 `task_summary.json` 里成功题的 skill 来源与失败题的错误族
- [ ] 在 `model3` 结束后，先基于自己对项目的理解和论文经验设计 skill 整合方式：
  - 不是机械去重，而是按题型、工具链、触发条件、输入选择策略和 stop condition 做聚类
  - 整合目标是得到 `6` 个高质量、可复用、字段风格统一的优良 skill
- [ ] 把整合后的 `6` 个 skill 直接送进评估框架测试：
  - 先看是否能稳定指导测试阶段的 router / planner / executor
  - 再看相较于当前分散 skill 池，是否在成功率、速度和稳定性上更优
  - 评估阶段默认按并行方案设计
- [ ] 如果整合后效果仍不理想，再考虑二次修正：
  - 是改整合方法
  - 还是改 skill 字段结构
  - 还是继续补少量 family-specific helper skill

## 实验结束后的重点分析

- [ ] 比较“原始 success-only skill 池”与“整合后的 6 个 skill 库”的表现差异
- [ ] 如有必要，再回看 `mode1 / mode2` 的训练表现、评估表现和最终 skill 库质量
- [ ] 观察训练过程中 `critic / actor` 的真实行为：
  - 是否每个 iteration 都会调用
  - 成功任务、失败任务、无匹配 skill 时各自怎么处理
  - 哪些路径会跳过 `critic` 或 `actor`
  - 产出的 reward / action selection / create / modify / merge 行为是否符合预期
- [ ] 检查整合后 skill 是否真的保留了 planner-oriented / executor-oriented 的有效先验，而不是只做表面压缩
- [ ] 判断后续主线是否彻底切到 `model3 + skill 整合 + 并行评估`

## 后续可能动作

- [ ] 评估“主流程 skill + helper modules”替代“单轮只选一个 skill”的可能性
- [X] 并发训练主线已转为 `model3`：
  - 先并行产出一条一条 success-only skill
  - 再在后处理阶段统一去重、合并和整合 skill 库
- [ ] 设计 skill 的降权 / 冻结 / 剔除 / 回退机制，避免坏 skill 反复命中
- [ ] 如果后续实验显示 prompt 复杂度收益不高，优先做 prompt 收缩，而不是继续堆叠 prompt

## 维护规则

- 这份文件只保留当前主线，不再重复列代表样本；样本保留理由统一写进 `实验记录/实验总表.md` 或对应实验记录
- run 有新增或结论有变化时，先更新 `实验记录/实验总表.md`
- 与双模式实验相关的长期观察，继续同步到 `AI交互记录/` 对应日期文件
- TODO 状态变化后，再同步到本文件或 `待办/已完成.md`
