# 已完成

这份文件只保留“已经做完、且后续还需要偶尔回看”的事项。更细的现象和逐 run 结果，统一以 `实验记录/实验总表.md` 为准。

## 代码与框架修复

- [x] critic 输入补上当前激活 skill 可调用工具的真实签名 / 参数 schema
  - 对应样本：`project_skills/runs/20260318_1938_q1_critic_tool_schema_fix`

- [x] 统一 EO 工具的相对输入路径解析
  - 对应样本：`project_skills/runs/20260318_2043_q1_relative_path_fix`

- [x] executor 只消费首个合法 JSON action，并兼容单次响应里的多 JSON / 低级格式噪声

- [x] 统一 EO 调用层支持 batched `compute_tvdi`，并阻断“列表被串成字符串”的低级失败

- [x] 修复 `calculate_tif_average` 对 `NaN` / `nodata` 的错误聚合
  - 对应成功样本：`project_skills/runs/20260319_1030_q1_statistics_fix_pass`
  - 对应连续通过样本：`project_skills/runs/20260319_1045_train5_after_statistics_fix_all_pass`

- [x] 确认 `GDAL runtime is not available in this environment` 的根因是运行目录 / 模块入口错误，而不是 GDAL 修复回退

- [x] 训练阶段成功 iteration 直接结束，不再额外调用 critic

## 已确认的运行经验

- [x] 长跑实验不要从可见 `cmd.exe` 窗口拉起
- [x] 这台机器上更稳的方式是直接用环境里的 `python.exe`
- [x] 如果只是保留 skill 库做测试，优先用 `evaluate-tasks`
- [x] 如果仍走训练流程但每题只试一轮，把 `runtime.max_iterations_per_task` 设成 `1`

## 当前保留的代表样本

- [x] `project_skills/runs/20260318_1938_q1_critic_tool_schema_fix`
- [x] `project_skills/runs/20260318_2043_q1_relative_path_fix`
- [x] `project_skills/runs/20260319_1030_q1_statistics_fix_pass`
- [x] `project_skills/runs/20260319_1045_train5_after_statistics_fix_all_pass`
- [x] `project_skills/runs/20260319_1125_train20_empty_library_monolithic_case`
- [x] `project_skills/runs/20260319_1535_train20_parallel03_best_9of20`
- [x] `project_skills/runs/20260319_2354_train20_qwen_executor_tpm_429`

## 说明

- 更细的“哪一题为什么成功 / 失败、关键配置是什么、观察到什么现象”，不要继续堆在这份文件里。
- 这些内容现在统一归档到 `实验记录/实验总表.md`。
