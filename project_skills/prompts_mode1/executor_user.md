Task payload:
{task_json}

Activated skill name:
{skill_name}

Activated SKILL.md:
{skill_md}

Bundled resources visible to you:
{resources_json}

Execution reminder:
- this is an executor-oriented skill, so follow its tool-order defaults closely
- resources listed above are files relative to the skill root
- execute `scripts/*.py` via `run_python_script`
- inspect `references/*.md` via `read_file`

Execute the task now. Follow the skill precisely, and only deviate when the observed tool results force you to.
