# VS Code 工作区存档

这里保存了本次从当前 VS Code 会话里提取出来的 3 个工作区。

打开方式：

- 在 VS Code 里直接打开对应的 `.code-workspace` 文件
- 或者在资源管理器里双击 `.code-workspace`

说明：

- `01` 和 `02` 都是单目录工作区，内容相同，都是当前仓库根目录
- `03` 是双目录工作区，包含当前仓库根目录和 `C:\Users\HP\Desktop\skills`

